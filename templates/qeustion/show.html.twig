{% extends 'base.html.twig' %}

{% block title %}Qeustion{% endblock %}

{% block body %}
    <h1>Qeustion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qeustion.id }}</td>
            </tr>
            <tr>
                <th>Question_text</th>
                <td>{{ qeustion.questionText }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('qeustion_index') }}">back to list</a>

    <a href="{{ path('qeustion_edit', {'id': qeustion.id}) }}">edit</a>

    {{ include('qeustion/_delete_form.html.twig') }}
{% endblock %}
